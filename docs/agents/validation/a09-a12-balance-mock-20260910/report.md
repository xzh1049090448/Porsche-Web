# A09–A12 本地限定验收报告

结论为 `PASS_LIMITED_SCOPE`。A09/A10 提供开发环境专用的 CNY 合成用户金额演示，A11 证明生产构建和后端写路由继续隔离真实金额能力，A12 复验既有 `users.delete` 软删除切片。生产迁移、部署、生产验收和真实业务账号均未运行。

## 范围结果

- A09：整数分计算、最多两位小数、上限、余额不足、增加/扣减/覆盖和永久 Mock 标记通过。
- A10：成功、失败、超时、版本冲突、同 key 单飞与完成后重放通过；失败不改变余额或记录。
- A11：`/demo/admin/balance` 只在 Vite 开发构建注册；生产产物扫描确认路由、模块标记和合成用户名均不存在。真实用户列表/详情保持“金额额度：未接入”，三条伪造调额路径保持 404。
- A12：当前前后端候选定向回归通过；结论仍限于软删除、重复删除隐藏、凭据失效和用户名占用，不包含恢复写链。

## 验证

- 前端 focused `8/8`、全量 `455/455`、生产构建和产物隔离扫描通过。
- 可见 Chromium 验证登录、开发入口、合成用户、成功、失败原子性、390px 布局和刷新重置。Mock 登录的既有后台 `/api/v1/auth/self` 请求因本地后端未启动产生两条 500 控制台噪声；金额演示本身没有调用真实 API 或数据库。
- 后端全量测试、构建及伪造调额路由 404 定向测试通过。
- 合并后的真实 MySQL 8 `0011–0013` 迁移和 `0013` A08 down 兼容验证通过。新的完整 A08 service fixture 复跑因一次 disposable MySQL host mapping 连接失败未闭环，因此继续引用原 A08 真实服务证据，并将本次结果单独标注。
- 三次 disposable fixture 均由退出 trap 精确删除；最终按 label 检查为零残留。

## 确认边界

本报告是本地代码与测试证据，尚未获得外部后端 `project_manager` 对 A09–A12 本轮候选的书面确认。A09/A10 为前端独立 Mock 范围；A11 后端边界由可执行 404 测试证明；A12 沿用此前联合验收结论并在当前候选复验。
