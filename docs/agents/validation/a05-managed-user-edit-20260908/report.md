# A05 Task7 r4 frontend joint-acceptance status

**Status: NOT_RUN_BLOCKED.** r4 replaces the prior r1–r3 blocked attempts as the current isolated-acceptance evidence; it is not a release or production approval.

The r4 backend gate is materially healthier than r2: a new MySQL 8.0/Redis 7.4 loopback fixture applied migrations 0001–0010, then passed 33 A05 service leaves with zero failures or skips. The repaired nil and non-nil no-op paths, hierarchy set/clear paths, rollback, concurrency, stale-identity/session, audit privacy, strict decoder, safe handler envelope, and exactly-one-route checks all pass. DTO/handler/router supplied 71 additional passing records with zero failures/skips; the A05 race and related service regression passed without a race report. The frontend `./init.sh` build also passed with its existing non-blocking Vite warnings.

The r4 runtime phase stopped before any server or visible Playwright browser was started. The one-shot Root bootstrap reported `Root bootstrap did not leave exactly one Root user`. An aggregate-only inspection of the r4 parent test database found synthetic residue from the related regression: 22 active User rows, 22 active Admin rows, two active Root rows, and two invalid-role rows. This cannot provide the required isolated exactly-one-Root runtime.

No Root-to-User/Admin, Admin-to-User, denial, conflict refresh/no-replay, keyboard/focus, responsive-width, direct-detail refresh, PATCH-body, network, console, adversarial, or privacy browser result is claimed. No browser credentials, tokens, request bodies, display-name values, usernames, screenshots, or console logs were collected. A successor run must provision a freshly migrated runtime database that has not been used by fixtures, then repeat all omitted runtime and browser gates before status promotion.
