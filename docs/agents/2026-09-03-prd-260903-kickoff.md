# PRD-260903 管理员用户管理与公共页面体系启动备忘录

## 2026-09-03：B1-C 双只读接口限定通过，隔离fixture已清理

- 后端 `PASS_LIMITED_SCOPE`：PM最终SPEC PASS与独立真实质量VERDICT PASS，四级安全问题均0、8生产hash二次匹配。独立focused96叶子PASS（service72/handler24）、race96叶子PASS、真实HTTP边界overlay1PASS，均0fail/0skip；writer full627/focused83/race107PASS及build/vet/diffPASS。
- 用户授权的本批测试生命周期已完成，任务MySQL/Redis exact ID/name/label/image复核后stop/AutoRemove，复查均不存在；本批fixture.env、随机凭据与私密目录已删除。报告、迁移ledger、日志及cleanup证据在BE `docs/superpowers/reports/validation/2026-09-03-b1c-admin-authz-read/real-fixture/`。
- 仅两项GET展示接口获得本地限定通过，合同entry仍AGREED_FOR_IMPLEMENTATION、implementation_status=PASS_LIMITED_SCOPE；overall DRAFT、其他25entry与根合同不变。不开放FE页面或旧权限写入，`web-012`不变，26项联合验收保持NOT_RUN，不涉及生产发布。
- 下方B1-C无fixture、待授权和待复核段落均已标记历史，不能作为当前阶段状态。


## 历史阶段 2026-09-03：B1-C 已完成真实fixture writer验证，待最终复核

- 用户对本批fixture生命周期已授权。后端实际完成MySQL8.0.46/Redis7.4.11、现有0001–0003迁移及隔离验证：focused83、race107、fresh full627 test PASS，0fail/0skip，15 package PASS/4 no-test；build/vet/diff PASS，8生产hash不变。
- 初次full只因归档.go被扫描和runner迁移APP_ENV外泄失败，证据更名.go.txt保留bytes并限定migration环境后fresh复跑；生产/断言/迁移未变，初次失败历史保留。
- PM DOCS ALIGNED无代码gap；独立真实fixture与PM最终结果复核待完成，任务fixture保留，go-015仍in_progress。下方静态/无fixture NOT_RUN与awaiting授权记录是历史；当前DB测试已执行，但不代表全PRD联合验收。
- FE仍仅两项只读合同AGREED，overallDRAFT、其他25entry与根合同不变，web-012不变，26项联合验收NOT_RUN；无业务页面或旧权限写入开放。


## 历史阶段 2026-09-03：B1-C r1 两个权限展示只读合同与本地候选

- 仅 `GET /admin/v2/authz/catalog` 与 `GET /admin/v2/users/{guid}/permissions` 达到 `AGREED_FOR_IMPLEMENTATION`；草案版本 `v0.2-draft-r3-b1c-r1`，overall 仍 `DRAFT`，其他25项接口及根 `interface-contract.json` 不变。
- 后端候选已实现 fresh actor/target/session SHARE 事务、Redis barrier、严格策略读取和 disabled Admin 展示投影。目录仅 active Admin/Root；详情仅 Root 查询非删除 Admin。认证失效401、角色不足403、隐藏目标404、不可用/腐败503，匹配路由参数不规范400；错误body只含 `detail`，请求ID在 `X-Request-ID` header，保留旧middleware401文案。
- 本轮无 fixture HTTP 验证为1 pass/5 skip/0 fail；纯投影与认证分类测试通过，真实 MySQL/Redis锁/提交/会话/策略测试尚未运行，等待本批隔离fixture生命周期授权。PM conditional SPEC CODE ALIGNED（条件为文档一致及真实fixture）；独立最终静态/无fixture复核已完成，VERDICT PARTIAL：四级安全问题均0、8生产hash匹配、纯JSON叶子18 PASS/34 fixture SKIP/0 FAIL，focused race、HTTP overlay probe及gofmt/diff/build/vet PASS；真实DB严格NOT_RUN，不能写成全批或联合验收通过。最终后端无fixture全量356 test pass/229 skip/0 fail、15 package pass/4 no-test，build/vet/race通过。
- FE只同步协调文档，不开放页面或旧权限写操作；`web-012` 保持 `not_started/awaiting_backend_integration`，26项联合验收均 `NOT_RUN`。


日期：2026-09-03。产品设计已获用户批准；本地 B1-A 内部 authz 实现已授权并完成限定验证。更广 HTTP/API 契约仍未冻结，不含生产部署、数据库迁移或整体验收批准。

## B1-B2 限定后端结果

后端 B1-B2 已获 PM SPEC PASS 与独立 `permission_snapshot_verify` VERDICT PASS（Critical/High/Medium/Low均none）。它只收紧既有 managed-user PUT/update：严格 JSON、真实 status/plan/用户 ACL 变化的会话撤销/AuthVersion/event10、daily-limit-only 审计和撤销事件 actor 归属。`allowed_models: []` 保持现有用户 ACL **unrestricted** 语义，且不等同 Gateway Key ACL；daily limit 0 的既有额度计算不变。该 `PASS_LIMITED_SCOPE` 不开放 FE 管理入口、不冻结更广 DTO/权限写入，也不替代26项联合验收或生产批准。

## B1-B3 Gateway Key 最新用户 ACL 协调状态

后端 B1-B3 已为 `PASS_LIMITED_SCOPE`：PM final SPEC PASS 与独立 VERDICT PASS，fresh JSON 544 pass/0 fail/0 skip。Gateway Key 每次请求读取 owner 最新已提交用户 ACL，必须同时满足 Key ACL 和 owner ACL；读取失败映射固定 503，owner 收窄在下一请求生效，且不承诺跨表原子时点或终止在途 SSE。`allowed_models: []` 保持 **unrestricted** 用户 ACL 语义。FE 不改业务代码，`web-012` 保持 `not_started/awaiting_backend_integration`，26项联合验收继续 `NOT_RUN`。

## 启动范围与基线

FE 工作树为 `/Users/xuzhihao/code/Porsche-Web/.worktrees/admin-public-260903`，分支 `feature/admin-public-260903`，基线 `25a66a4ad546a481941dfc6e0cc9bc75da2e631b`。BE 同名工作树基线为 `aec1619ee710c80cd71dbe529660e2d12b3fda7b`，分支同为 `feature/admin-public-260903`。主 PRD 为 `PRD-260903` v0.2，输入 SHA256 为 `58ab6fa63c9e5bcc9704ecc2167c1dfe75b77ff9ca0c8117f834fe88333eb31a`。

视觉参考已固定为设计 PRD v1.1 及三个只读 HTML 原型：landing `9be27fce21a8b0f2cd882341819a88b91ef29ebf066a7d3db0c395cef10c71da`、pricing `29a3ecb84f9cdac34867b162e57a2c7bd8bfc36e95cf8e956816f13712259b07`、users `cd12ed57d0f9f7fc48684992af19d9a45b1c4fde6173d58b769ceeda4e9fbee2`。原型只提供颜色、排版、布局和交互参考，不提供真实数据或业务权限。

已确认：D01 仅软删除、不恢复、用户名永久保留；D02 额度是金额余额，本期允许隔离 Mock，不改每日调用限制；D04 角色层级和 Root 硬边界；D05 继续 Vue 技术体系。旧 P0/web-009 保持 `in_progress`，M3 仍为 `PARTIAL`，历史生成预算 4/4 已耗尽；历史 PASS 不作为本分支新测试证据。

## 用户已确认的产品决策组

1. 用户已确认 `/` 公共官网、`/chat` 对话入口；价格默认公开并保留受控统一开关建议。
2. 用户已确认角色基线加用户覆盖、拒绝优先、敏感动作五分钟单次票据、权限变化递增 AuthVersion 并撤销目标会话的安全方向。
3. 用户已确认排除真实金额账本/支付、Passkey/OAuth/2FA/PAT、物理删除/恢复、全量系统设置、自定义角色和批量危险操作。

不重复询问 D01、D02、D04、D05。上述确认仍不冻结技术 DTO 或实现验收；User 无 email 仍是基线差异。

## 推荐开发顺序

- B0：冻结管理/公共 DTO、错误、能力、版本与发布安全设计。
- B1：账户授权、审计、会话失效和动作票据底座。
- B2：账户列表、详情、创建、编辑、启停、软删除；同时修复旧 `/admin/users` 绕过入口。
- B3：管理 UI、真实分组、会话、权限覆盖和联调。
- B4：公共数据、草稿、预览、发布快照和历史版本。
- B5：公共页面、SEO、价格可见性和缓存门禁。
- B6：前后端联合回归、质量复核、发布与回滚演练。

B1 作为独立首批管理底座设计和评审单元，不与公共页面或金额 Mock 绑定发布。提交后未知状态必须通过只读查询路径和原幂等键重查；若后端未提供查询路径则在 B0 阻断，不向页面报告成功。能力三态固定为 `inherit/allow/deny`；时间序列化按各 DTO 单独冻结，不把数据库 Unix 毫秒强制套用所有 API。

后端 PM 已书面接受设计方向：共享账户动作服务与独立 authz 模块；Casbin 仅候选，当前不装依赖。授权判断顺序为 actor 最新状态 → Root/self 硬边界 → 可见性 → 注册能力 → baseline+override，拒绝优先，unknown 与生产 `users.quota.adjust` always deny。旧 admin/users PUT 的绑定错误/未知字段和旧 admin/logs 审计范围需修复，不能成为绕过入口。现有 User 无 email，前端不承诺邮箱搜索。

认证响应可选统一投影 `admin_permissions` 与 `permissions_version`；缺失时前端不给管理能力。用户状态、权限、plan、group、model ACL、password reset 变更统一触发 AuthVersion/会话与 Gateway ACL 失效，DB 最新态读取失败必须 fail closed，outbox 仅传播。B0 还需审查旧 `/admin/users`、`/admin/logs`、`/admin/logs/alerts`、`alerts/dashboard` 及其子路由等入口；已知任意 Admin 可读写全局告警的风险必须在新 capability 方案中关闭或退役，本期不扩展全量系统设置。

T01 票据创建 admin 的技术建议已获 PM 书面接受：复核接口先建立 intent；`POST /admin/v2/users` 在事务内消费既有 intent/ticket，并创建账户、初始化权益权限、审计、幂等结果和 outbox；普通 user 创建无需 intent。创建意图不创建用户、不占用户名；服务端 HMAC 覆盖规范化字段和原样密码；密码不 trim、不落盘；重复成功重查只返回既有结果，不重复执行。最终字段和 revision 仍待回执。

T02 公共价格切换技术建议已获 PM 书面接受：Vue 预渲染配 Go 统一门禁，跨实例持久化 switching 和协调；先阻止旧策略的新响应、排空/中止登记响应，所有节点、源站、HEAD、Range、304、历史快照和 CDN 路径均校验；旧缓存证明失效后再原子提交策略。失败进入可恢复阻断状态，不报成功。报价 `no-store`，保护壳不带报价元信息并 `noindex`，匿名不触发 Refresh，保持 Cookie Path。

金额隔离的发布门禁不能只依赖现有 `VITE_USE_MOCK` warning：生产 build/deploy 必须硬拒绝 Mock 配置，产物不含演示入口和合成数据；该要求纳入 A11/R02。

## 责任与证据

前端负责人负责页面、store、服务适配和浏览器证据；后端 PM 负责 DTO、授权、事务、失效、审计、发布门禁和运行证据；协调者负责双方版本、变更和签收；独立质量角色负责最终复核。管理动作、旧入口、能力和审计必须以真实接口和持久化结果验收；金额 Mock 单独报告。

当前新需求的 A01-A14、P01-P08、V01-V02、R01-R02 共 26 项全部为 `NOT_RUN`。不使用未经确认日期，采用准入依赖推进。日同步仅在活跃会话中进行，不创建后台自动化。

## 明确不执行

FE 本轮不改业务代码、不安装依赖、不运行 FE 测试或 build、不改 schema、不迁移/部署、不调用模型、不读生产凭证、不提交或推送；B1-A BE 纯库实现及其限定验证已完成，后续仍须契约审核后推进更广 B1。

## B1-A 内部协调任务（2026-09-03）

后端 PM 已提出、前端协调者接受 B1-A：仅由 BE 实施内部 `authz` 纯策略评估器切片，依据 BE 计划中的 24 项 capability、immutable evaluator、User/Create/Collection/Resource 四入口、默认 deny、显式 deny 优先及 Root/self/peer 硬边界。模块不接 HTTP、DTO 或 DB；认证 projection 不交付，FE 不改业务且不开放管理入口。focused/full/package/build/vet 与独立并发验证均已限定 PASS；此结果不改变公共接口 DRAFT，不等于安全失效或实际权限落地。

B1-B1 已由后端 PM 提出、前端协调者接受并委派实施，现为限定 `PASS_LIMITED_SCOPE`：仅处理 `user_permission_heads`/`user_permission_overrides` 快照持久化与 `LoadPermissionSnapshot(ctx, rootDB, userID)` 内部加载。PM SPEC PASS；独立 permission_snapshot_verify fresh build/vet/diff/json 均 0，real fixture full 456 pass/0 skip/0 fail，反向锁 probe PASS 1.362s，SECURITY_REPORT 四级风险均 none、VERDICT PASS。无 HTTP、DTO、policy 写接口或 FE 接线；HTTP `permissions_version` 仍未冻结，完整 B1 未完成。
