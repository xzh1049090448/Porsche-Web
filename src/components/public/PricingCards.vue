<script setup>
import { formatPublicPrice } from '@/utils/public-catalog.js'
defineProps({ models: { type: Array, required: true } })
const linkFor = model => `/pricing/${encodeURIComponent(model.modelKey)}`
</script>
<template><div class="pricing-cards"><article v-for="model in models" :key="model.modelKey" class="pricing-card"><header><div><h2><RouterLink :to="linkFor(model)">{{ model.displayName }}</RouterLink></h2><code>{{ model.modelKey }}</code></div><span>{{ model.provider }}</span></header><dl><div><dt>输入</dt><dd>{{ formatPublicPrice(model.inputPrice).label }}<small v-if="model.inputPrice !== undefined">USD / 百万 Token</small></dd></div><div><dt>输出</dt><dd>{{ formatPublicPrice(model.outputPrice).label }}<small v-if="model.outputPrice !== undefined">USD / 百万 Token</small></dd></div></dl><p><span v-for="item in model.capabilities" :key="item" class="pricing-tag">{{ item }}</span></p></article></div></template>
<style scoped>.pricing-cards{display:none;gap:14px}.pricing-card{padding:20px;border:1px solid var(--public-border);border-radius:16px;background:var(--public-card)}.pricing-card header{display:flex;justify-content:space-between;gap:16px}.pricing-card h2{margin:0 0 5px;font-size:19px}.pricing-card code,.pricing-card header>span{color:var(--public-muted);font-size:13px}.pricing-card dl{display:grid;grid-template-columns:1fr 1fr;gap:12px}.pricing-card dl>div{padding:12px;border-radius:10px;background:var(--public-bg)}.pricing-card dt{color:var(--public-muted);font-size:13px}.pricing-card dd{margin:6px 0 0;font-weight:800}.pricing-card small{display:block;margin-top:4px;color:var(--public-muted);font-weight:400}.pricing-tag{display:inline-block;margin:0 5px 5px 0;padding:3px 8px;border-radius:999px;background:#eff6ff;color:#1d4ed8}.pricing-card a:focus-visible{outline:3px solid var(--public-primary);outline-offset:3px}@media(max-width:767px){.pricing-cards{display:grid}}/* 375px mobile acceptance */</style>
