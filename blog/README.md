# Porsche Web 博客示例

轻量级原生 HTML/CSS/JS 博客前端，对接后端 `http://localhost:3000/api`。

## 运行

用任意静态服务器打开本目录，或直接打开 `index.html`（需后端支持 CORS）。

```bash
# 示例：npx serve blog
npx serve .
```

## 文件

- `index.html` — 页面结构
- `styles.css` — 样式
- `app.js` — 逻辑与 API 调用
